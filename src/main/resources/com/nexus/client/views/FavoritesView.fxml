<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<VBox fx:id="rootContainer"
      xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.nexus.client.controller.FavoritesController"
      styleClass="content-view">

    <!-- Top Header/Toolbar -->
    <HBox styleClass="view-header" alignment="CENTER_LEFT" spacing="16">
        <padding><Insets top="0" right="32" bottom="0" left="32"/></padding>

        <HBox alignment="CENTER_LEFT" spacing="16" HBox.hgrow="ALWAYS">
            <ImageView fitWidth="20" fitHeight="20" preserveRatio="true">
                <Image url="@../../../../assets/favorite.png"/>
            </ImageView>
            <Label text="Favorites" styleClass="view-title"/>
            <Label fx:id="gameCountLabel" text="0 GAMES" styleClass="game-count-badge"/>
        </HBox>
    </HBox>

    <!-- Content Area -->
    <StackPane VBox.vgrow="ALWAYS">

        <!-- Empty State -->
        <VBox fx:id="emptyState" styleClass="empty-state" alignment="CENTER" spacing="16">
            <ImageView fitWidth="48" fitHeight="48" preserveRatio="true" opacity="0.5">
                <Image url="@../../../../assets/favorite.png"/>
            </ImageView>
            <Label text="No Favorites Yet" styleClass="empty-title"/>
            <Label text="Right click a game to add it to favorites." styleClass="empty-description"/>
        </VBox>

        <!-- Favorites Grid -->
        <ScrollPane styleClass="games-scroll-pane" fitToWidth="true">
            <VBox alignment="TOP_CENTER" styleClass="games-grid-container">
                <FlowPane fx:id="favoritesGrid"
                          styleClass="games-grid"
                          hgap="24"
                          vgap="24"
                          alignment="CENTER">
                    <padding><Insets top="32" right="32" bottom="32" left="32"/></padding>
                </FlowPane>
            </VBox>
        </ScrollPane>

    </StackPane>

</VBox>

